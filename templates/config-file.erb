################################################################################
#                     THIS FILE IS UNDER PUPPET CONTROL!                       #
################################################################################
<%
if @config and !@config.empty? and @config.is_a?(Hash)
  @config.sort.each { |key, value|
    %><%= "#{key} = #{value}\n" %><%
  }
else
  %>
#
# WARNING:
# Configuration data is missing or of an incorrect data type.
#
  <%
end %>
